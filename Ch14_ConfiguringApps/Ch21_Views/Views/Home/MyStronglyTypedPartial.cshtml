@model IEnumerable<string>

<div class="bg-info">
    This is the message from partial view.
    <ul>
        @foreach (string str in Model)
        {
            <li>@str</li>
        }
    </ul>
</div>